# SQL-Data-Cleaning-Standardization-Pipeline
This project demonstrates a complete SQL‑based data‑cleaning workflow applied to a messy dataset containing inconsistent formatting, embedded symbols, bracketed values, and non‑numeric characters inside numeric fields. The goal was to transform the raw file into a clean, analysis‑ready table.
